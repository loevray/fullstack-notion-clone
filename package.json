{
  "name": "backend-start",
  "version": "1.0.0",
  "description": "empty",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev:api-server\" \"npm run dev:web-server\"",
    "dev:api-server": "npm run dev --prefix api-server",
    "dev:web-server": "npm run dev --prefix web-server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.0"
  }
}
